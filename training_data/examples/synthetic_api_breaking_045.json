{
  "before_code": "/* flash_storage.h - Internal Flash Storage API */\n#ifndef FLASH_STORAGE_H\n#define FLASH_STORAGE_H\n\n#include <stdint.h>\n#include <stddef.h>\n\n#define FLASH_PAGE_SIZE      256U\n#define FLASH_SECTOR_SIZE    4096U\n#define FLASH_BASE_ADDRESS   0x08000000U\n\ntypedef enum {\n    FLASH_OK          = 0,\n    FLASH_ERR_TIMEOUT = -1,\n    FLASH_ERR_PROTECT = -2,\n    FLASH_ERR_ALIGN   = -3,\n    FLASH_ERR_VERIFY  = -4\n} flash_status_t;\n\nflash_status_t flash_write(uint32_t address, const uint8_t *data, size_t length);\nflash_status_t flash_read(uint32_t address, uint8_t *buffer, size_t length);\nflash_status_t flash_erase_sector(uint32_t sector_address);\nflash_status_t flash_erase_chip(void);\nuint32_t       flash_get_size(void);\n\n#endif /* FLASH_STORAGE_H */",
  "after_code": "/* flash_storage.h - Internal Flash Storage API */\n#ifndef FLASH_STORAGE_H\n#define FLASH_STORAGE_H\n\n#include <stdint.h>\n#include <stddef.h>\n\n#define FLASH_PAGE_SIZE      256U\n#define FLASH_SECTOR_SIZE    4096U\n#define FLASH_BASE_ADDRESS   0x08000000U\n\ntypedef enum {\n    FLASH_OK          = 0,\n    FLASH_ERR_TIMEOUT = -1,\n    FLASH_ERR_PROTECT = -2,\n    FLASH_ERR_ALIGN   = -3,\n    FLASH_ERR_VERIFY  = -4\n} flash_status_t;\n\nflash_status_t flash_write(uint32_t address, const uint8_t *data, size_t length);\nflash_status_t flash_read(uint32_t address, uint8_t *buffer, size_t length);\nflash_status_t flash_erase_sector(uint32_t sector_address);\n/* BREAKING: flash_erase_chip removed - use flash_erase_range(FLASH_BASE_ADDRESS, flash_get_size()) */\nflash_status_t flash_erase_range(uint32_t start_address, uint32_t length);\nuint32_t       flash_get_size(void);\n\n#endif /* FLASH_STORAGE_H */",
  "patch": "--- a/firmware.c\n+++ b/firmware.c\n@@ -20,7 +20,8 @@\n flash_status_t flash_write(uint32_t address, const uint8_t *data, size_t length);\n flash_status_t flash_read(uint32_t address, uint8_t *buffer, size_t length);\n flash_status_t flash_erase_sector(uint32_t sector_address);\n-flash_status_t flash_erase_chip(void);\n+/* BREAKING: flash_erase_chip removed - use flash_erase_range(FLASH_BASE_ADDRESS, flash_get_size()) */\n+flash_status_t flash_erase_range(uint32_t start_address, uint32_t length);\n uint32_t       flash_get_size(void);\n \n #endif /* FLASH_STORAGE_H */",
  "bug_category": "api_breaking",
  "language": "c",
  "before_ref": "synthetic",
  "synthetic": true,
  "patch_hash": "5689e4da6df1b84b22be4854deef5b4f2e03c0cbaeb568dd37e66e379b8db9ab"
}