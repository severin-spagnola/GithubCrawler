{
  "before_code": "/* flash_storage.h - Internal Flash Storage Driver */\n#ifndef FLASH_STORAGE_H\n#define FLASH_STORAGE_H\n\n#include <stdint.h>\n\n#define FLASH_PAGE_SIZE   256U\n#define FLASH_SECTOR_SIZE 4096U\n\ntypedef struct {\n    uint32_t base_addr;\n    uint32_t total_size;\n    uint16_t page_size;\n    uint16_t sector_size;\n    uint8_t  write_granularity; /* minimum write size in bytes */\n} flash_info_t;\n\nint  flash_init(void);\nvoid flash_get_info(flash_info_t *info);\nint  flash_read(uint32_t addr, uint8_t *buf, uint32_t len);\nint  flash_write(uint32_t addr, const uint8_t *buf, uint32_t len);\nint  flash_erase_sector(uint32_t sector_addr);\nint  flash_erase_chip(void);\n\n#endif /* FLASH_STORAGE_H */",
  "after_code": "/* flash_storage.h - Internal Flash Storage Driver */\n#ifndef FLASH_STORAGE_H\n#define FLASH_STORAGE_H\n\n#include <stdint.h>\n\n#define FLASH_PAGE_SIZE   256U\n#define FLASH_SECTOR_SIZE 4096U\n\ntypedef struct {\n    uint32_t base_addr;\n    uint32_t total_size;\n    uint16_t page_size;\n    uint16_t sector_size;\n    uint8_t  write_granularity; /* minimum write size in bytes */\n} flash_info_t;\n\nint  flash_init(void);\nvoid flash_get_info(flash_info_t *info);\nint  flash_read(uint32_t addr, uint8_t *buf, uint32_t len);\nint  flash_write(uint32_t addr, const uint8_t *buf, uint32_t len);\nint  flash_erase_sector(uint32_t sector_addr);\n\n/* flash_erase_chip() removed for safety; use flash_erase_range() instead */\nint  flash_erase_range(uint32_t start_addr, uint32_t end_addr);\n\n#endif /* FLASH_STORAGE_H */",
  "patch": "--- a/firmware.c\n+++ b/firmware.c\n@@ -20,6 +20,8 @@\n int  flash_read(uint32_t addr, uint8_t *buf, uint32_t len);\n int  flash_write(uint32_t addr, const uint8_t *buf, uint32_t len);\n int  flash_erase_sector(uint32_t sector_addr);\n-int  flash_erase_chip(void);\n+\n+/* flash_erase_chip() removed for safety; use flash_erase_range() instead */\n+int  flash_erase_range(uint32_t start_addr, uint32_t end_addr);\n \n #endif /* FLASH_STORAGE_H */",
  "bug_category": "api_breaking",
  "language": "c",
  "before_ref": "synthetic",
  "synthetic": true,
  "patch_hash": "9956d6671cd2140a81fb22713dec4c300b4f46024a29ef0646f6565af215057d"
}