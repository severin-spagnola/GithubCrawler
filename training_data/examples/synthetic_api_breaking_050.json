{
  "before_code": "/* dma_controller.h - DMA Controller Driver */\n#ifndef DMA_CONTROLLER_H\n#define DMA_CONTROLLER_H\n\n#include <stdint.h>\n#include <stdbool.h>\n#include <stddef.h>\n\n#define DMA_MAX_CHANNELS     16U\n#define DMA_FLAG_CIRCULAR    (1U << 0)\n#define DMA_FLAG_MEM_TO_MEM  (1U << 1)\n#define DMA_FLAG_PERIPH_INC  (1U << 2)\n#define DMA_FLAG_MEM_INC     (1U << 3)\n\ntypedef void (*dma_complete_cb_t)(uint8_t channel, bool error);\n\ntypedef struct {\n    uint32_t src_address;\n    uint32_t dst_address;\n    uint16_t transfer_size;\n    uint8_t  data_width;\n    uint8_t  priority;\n    uint32_t flags;\n    dma_complete_cb_t callback;\n} dma_config_t;\n\nint  dma_channel_init(uint8_t channel, const dma_config_t *cfg);\nvoid dma_channel_start(uint8_t channel);\nvoid dma_channel_stop(uint8_t channel);\nbool dma_channel_is_busy(uint8_t channel);\nvoid dma_channel_deinit(uint8_t channel);\n\n#endif /* DMA_CONTROLLER_H */",
  "after_code": "/* dma_controller.h - DMA Controller Driver */\n#ifndef DMA_CONTROLLER_H\n#define DMA_CONTROLLER_H\n\n#include <stdint.h>\n#include <stdbool.h>\n#include <stddef.h>\n\n#define DMA_MAX_CHANNELS     16U\n#define DMA_FLAG_CIRCULAR    (1U << 0)\n#define DMA_FLAG_MEM_TO_MEM  (1U << 1)\n#define DMA_FLAG_PERIPH_INC  (1U << 2)\n#define DMA_FLAG_MEM_INC     (1U << 3)\n\n/* BREAKING: callback signature changed - added transfer_count parameter */\ntypedef void (*dma_complete_cb_t)(uint8_t channel, bool error, uint16_t transfer_count);\n\ntypedef struct {\n    uint32_t src_address;\n    uint32_t dst_address;\n    uint16_t transfer_size;\n    uint8_t  data_width;\n    uint8_t  priority;\n    uint32_t flags;\n    dma_complete_cb_t callback;\n} dma_config_t;\n\nint  dma_channel_init(uint8_t channel, const dma_config_t *cfg);\nvoid dma_channel_start(uint8_t channel);\nvoid dma_channel_stop(uint8_t channel);\nbool dma_channel_is_busy(uint8_t channel);\nvoid dma_channel_deinit(uint8_t channel);\n\n#endif /* DMA_CONTROLLER_H */",
  "patch": "--- a/firmware.c\n+++ b/firmware.c\n@@ -12,7 +12,8 @@\n #define DMA_FLAG_PERIPH_INC  (1U << 2)\n #define DMA_FLAG_MEM_INC     (1U << 3)\n \n-typedef void (*dma_complete_cb_t)(uint8_t channel, bool error);\n+/* BREAKING: callback signature changed - added transfer_count parameter */\n+typedef void (*dma_complete_cb_t)(uint8_t channel, bool error, uint16_t transfer_count);\n \n typedef struct {\n     uint32_t src_address;\n",
  "bug_category": "api_breaking",
  "language": "c",
  "before_ref": "synthetic",
  "synthetic": true,
  "patch_hash": "9a8fb73890512afabfa15b754a72f241d2192c91a614b5795131a52ce6ab41f6"
}