{
  "before_code": "/* spi_driver.h - SPI Driver Interface */\n#ifndef SPI_DRIVER_H\n#define SPI_DRIVER_H\n\n#include <stdint.h>\n#include <stdbool.h>\n\n#define SPI_FLAG_CPOL     (1U << 0)\n#define SPI_FLAG_CPHA     (1U << 1)\n#define SPI_FLAG_LSB_FIRST (1U << 2)\n#define SPI_FLAG_DMA_EN   (1U << 3)\n\ntypedef struct spi_device spi_device_t;\n\ntypedef void (*spi_callback_t)(spi_device_t *dev, uint32_t status);\n\nstruct spi_device {\n    uint8_t        bus_id;\n    uint8_t        cs_pin;\n    uint32_t       speed_hz;\n    uint16_t       flags;\n    spi_callback_t on_complete;\n    void          *user_data;\n};\n\nbool spi_open(spi_device_t *dev);\nvoid spi_close(spi_device_t *dev);\nint  spi_transfer(spi_device_t *dev, const uint8_t *tx, uint8_t *rx, uint16_t len);\n\n#endif /* SPI_DRIVER_H */",
  "after_code": "/* spi_driver.h - SPI Driver Interface */\n#ifndef SPI_DRIVER_H\n#define SPI_DRIVER_H\n\n#include <stdint.h>\n#include <stdbool.h>\n\n#define SPI_FLAG_CPOL     (1U << 0)\n#define SPI_FLAG_CPHA     (1U << 1)\n#define SPI_FLAG_LSB_FIRST (1U << 2)\n#define SPI_FLAG_DMA_EN   (1U << 3)\n\ntypedef struct spi_device spi_device_t;\n\ntypedef void (*spi_callback_t)(spi_device_t *dev, uint32_t status);\n\nstruct spi_device {\n    uint8_t        bus_id;\n    uint8_t        cs_pin;\n    uint8_t        cs_active_low;  /* NEW FIELD inserted before speed_hz */\n    uint8_t        word_size;      /* NEW FIELD inserted before speed_hz */\n    uint32_t       speed_hz;\n    uint16_t       flags;\n    spi_callback_t on_complete;\n    void          *user_data;\n};\n\nbool spi_open(spi_device_t *dev);\nvoid spi_close(spi_device_t *dev);\nint  spi_transfer(spi_device_t *dev, const uint8_t *tx, uint8_t *rx, uint16_t len);\n\n#endif /* SPI_DRIVER_H */",
  "patch": "--- a/firmware.c\n+++ b/firmware.c\n@@ -17,6 +17,8 @@\n struct spi_device {\n     uint8_t        bus_id;\n     uint8_t        cs_pin;\n+    uint8_t        cs_active_low;  /* NEW FIELD inserted before speed_hz */\n+    uint8_t        word_size;      /* NEW FIELD inserted before speed_hz */\n     uint32_t       speed_hz;\n     uint16_t       flags;\n     spi_callback_t on_complete;\n",
  "bug_category": "api_breaking",
  "language": "c",
  "before_ref": "synthetic",
  "synthetic": true,
  "patch_hash": "7d43743344927e8e3c14e592f1de07ae725cb09a988f94e432c6ca020ae4eefd"
}