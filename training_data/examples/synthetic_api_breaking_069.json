{
  "before_code": "#ifndef CAN_DRV_H\n#define CAN_DRV_H\n\n#include <stdint.h>\n\ntypedef struct {\n    uint32_t id;\n    uint8_t  dlc;\n    uint8_t  data[8];\n} can_frame_t;\n\nint  can_init(uint8_t bus, uint32_t bitrate);\nint  can_send(uint8_t bus, const can_frame_t *frame);\nint  can_recv(uint8_t bus, can_frame_t *frame, uint32_t timeout_ms);\nvoid can_close(uint8_t bus);\n\n#endif",
  "after_code": "#ifndef CAN_DRV_H\n#define CAN_DRV_H\n\n#include <stdint.h>\n\ntypedef struct {\n    uint32_t id;\n    uint8_t  flags;\n    uint8_t  dlc;\n    uint8_t  data[8];\n} can_frame_t;\n\nint  can_init(uint8_t bus, uint32_t bitrate);\nint  can_send(uint8_t bus, const can_frame_t *frame);\nint  can_recv(uint8_t bus, can_frame_t *frame, uint32_t timeout_ms);\nvoid can_close(uint8_t bus);\n\n#endif",
  "patch": "--- a/firmware.c\n+++ b/firmware.c\n@@ -5,6 +5,7 @@\n \n typedef struct {\n     uint32_t id;\n+    uint8_t  flags;\n     uint8_t  dlc;\n     uint8_t  data[8];\n } can_frame_t;\n",
  "bug_category": "api_breaking",
  "language": "c",
  "before_ref": "synthetic",
  "synthetic": true,
  "description": "A new 'flags' field was inserted between 'id' and 'dlc' in can_frame_t, shifting member offsets and breaking all code that accesses the struct by offset or initializes it with positional initializers.",
  "patch_hash": "59f3806229aa2388965295b239740c9d4f0de4da88733ed1a3e6a93060374011"
}